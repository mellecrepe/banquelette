{
   "_id": "_design/all",
   "language": "javascript",
   "views": {
       "by_date": {
           "map": "function(doc) {\n  emit(doc.date, doc);\n}"
       },
       "check_false": {
           "map": "function(doc) {\n  if ( doc.check == false ){\n    emit(doc.date, doc);\n  }\n}"
       }
   }
}
